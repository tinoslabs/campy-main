"""Campy AI settings package.

* ``base``  — shared configuration
* ``dev``   — local development (SQLite, debug on)
* ``prod``  — production (Postgres, Redis, security headers)
* ``test``  — fast settings used by the test-suite
"""
